import { RoomOptions } from "./room-options";

// temporary until I create something real with the backend
export type FakeRoom = {
    id: string;
    hostNickname: string;
    options: RoomOptions;
};
